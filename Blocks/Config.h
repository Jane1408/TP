#pragma once
#include <SFML\Graphics.hpp>

static const sf::Vector2f START_POSITION = { 300, 20 };

static const sf::Vector2f SIZE = { 50, 30 };

static const sf::Vector2f WINDOW_SIZE = { 800, 600 };

static const int NUMBER_BRICKS = 6;

static const float SPACE = 10;

static const float SPEED = 0.1f;

static const float DELTA = 0.1f;

static const float ZOOM = 0.01f;

static const float ROTATE = 0.1f;